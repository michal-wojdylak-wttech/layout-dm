import os

ROOT = os.path.join("./", "download")
KMEANS_WEIGHT_ROOT = os.path.join(ROOT, "clustering_weights")
DATASET_DIR = os.path.join(ROOT, "datasets")
FID_WEIGHT_DIR = os.path.join(ROOT, "fid_weights", "FIDNetV3")
JOB_DIR = os.path.join(ROOT, "pretrained_weights")
